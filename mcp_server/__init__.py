"""MCP server exposing the voc-amazon-reviews skill to MCP clients (Claude
Desktop, Claude Code, Cursor, Windsurf, etc.).

Public API: `mcp_server.server.mcp` — the FastMCP instance with all 4 tools
registered. Run as: `python -m mcp_server.server`.
"""
