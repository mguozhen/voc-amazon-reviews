"""Pure-Python clients for external services (Shulex VOC API, Anthropic).

Replaces the legacy fetch.sh / analyze.sh / voc.sh shell scripts so the
package can ship cleanly on PyPI without needing bash, curl, or openclaw
installed on the user's machine.
"""
